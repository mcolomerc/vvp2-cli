metadata:
  name: kubernetes-target
  namespace: production
  labels:
    environment: production
    type: kubernetes
  annotations:
    description: "Kubernetes deployment target for production workloads"
spec:
  kubernetes:
    namespace: "flink-jobs"
